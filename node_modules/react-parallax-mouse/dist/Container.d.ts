import React from "react";
import { SpringConfig } from "react-spring";
interface Props {
    globalFactorX?: number;
    globalFactorY?: number;
    resetOnLeave?: boolean;
    useWindowMouseEvents?: boolean;
    inverted?: boolean;
    springConfig?: SpringConfig;
    enabled?: boolean;
    containerStyle?: React.CSSProperties;
    className?: string;
    children: JSX.Element[] | JSX.Element;
}
declare const MouseParallaxContainer: ({ globalFactorX, globalFactorY, resetOnLeave, useWindowMouseEvents, inverted, springConfig, enabled, containerStyle, className, children, }: Props) => React.JSX.Element;
export { MouseParallaxContainer };
